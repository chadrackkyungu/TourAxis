import { createUserService, updateUserService, getAllUserService, getUserService } from '../services';

export default {
    createUserService,
    updateUserService,
    getAllUserService,
    getUserService
}
